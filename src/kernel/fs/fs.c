// File System // ~ eylon

#include <kernel/fs.h>
#include <drivers/pata.h>
#include <libc/stddef.h>

void init_fs()
{
    
}

// fs_read();          // read from a file descriptor
// fs_write();         // write to a file descriptor
// fs_open();          // open and possibly create a file
// fs_close();         // close a file descriptor
// fs_creat();         // create a file
// fs_lseek();         // reposition read/write file offset
// fs_mkdir();         // create a directory
// fs_rmdir();         // delete a directory
// fs_unlink();        // delete a file
// fs_rename();        // change the name or location of a file
// fs_chmod();         // change permissions of a file
// fs_stat();          // get file status by file descriptor
// fs_fstat();         // get file status by file name
// fs_truncate();      // truncate a file to a specified length by file descriptor
// fs_ftruncate();     // truncate a file to a specified length by file name